# Contacts Backend API

PHP Laravel project set up as API REST to solve the request incoming from Contacts React App.

## Steps for running Laravel project

Download and Install packages
`composer install`

Run command:
`php artisan serve`
